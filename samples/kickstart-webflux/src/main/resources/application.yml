spring:
  main:
    web-application-type: reactive
  output:
    ansi:
      enabled: always

graphql:
  graphiql:
    enabled: true
  datetime:
    scalars:
      date:
        scalar-name: MyDate
      local-date:
        scalar-name: MyLocalDate
